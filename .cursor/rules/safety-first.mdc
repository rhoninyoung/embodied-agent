---
description: 物理操作安全规范 — 所有涉及硬件控制的代码必须遵守
globs: src/control/**,src/middleware/**
alwaysApply: false
---

# 安全规范

## 硬性要求

- 所有运动指令必须经过工作空间边界检查
- 夹爪力矩不得超过配置阈值
- 机械臂速度在接近物体时必须自动降档
- 每个控制函数必须支持急停回调
- 串口通信超时必须触发安全停止而非重试

## 代码模式

- 运动函数签名必须包含 `safety_check: bool = True` 参数
- 禁止在安全检查未通过时用 `force=True` 绕过
- 所有异常处理中必须先执行安全停止，再处理异常
